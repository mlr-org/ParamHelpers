#' @param dict (`environment` | [list] | `NULL`)\cr
#'   Environment or list which will be used for evaluating the variables of
#'   expressions within a parameter, parameter set or list of parameters. The
#'   default is \code{NULL}.
